# Env and secrets
.env
.env.*
!.env.example
transportistas_sync/.env
transportistas_sync/.env.*
*.env.local

# Virtual env
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# SAM / deploy secrets
.aws-sam/
packaged.yaml
samconfig.local.toml
samconfig.*.local.toml
lambda-deploy.zip

# Lambda invoke / payload dumps (may contain data; keep out of git)
.lambda-invoke-*.json
.lambda-invoke-*.log
.lambda-payload-*.json

# Node.js
node_modules/
npm-debug.log*

# Vercel (local project link; use dashboard or re-link in CI)
.vercel/

# OS / editor
.DS_Store
*.swp
